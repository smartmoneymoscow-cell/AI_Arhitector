fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0
pydantic>=2.5.0
# Geometry (легковесные)
shapely>=2.0.0
networkx>=3.0
# Image processing
Pillow>=10.0.0
# BIM — тяжёлый, ставим отдельно
# ifcopenshell часто не ставится через pip на slim образах
# Раскомментировать если нужен IFC:
# ifcopenshell>=0.7.0
